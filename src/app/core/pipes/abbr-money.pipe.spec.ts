import { AbbrMoneyPipe } from './abbr-money.pipe';

describe('AbbrMoneyPipe', () => {
  it('create an instance', () => {
    const pipe = new AbbrMoneyPipe();
    expect(pipe).toBeTruthy();
  });
});
